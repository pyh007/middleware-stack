# 索引分片路由与别名练习题

先闭卷作答，再看实验证据和 `answers.md`。

1. 一个写请求从协调节点到主分片、副本再返回客户端经历哪些步骤？
2. 主分片数为什么是重要的长期决策？split、shrink 或 reindex 各有什么边界？
3. 自定义 routing 为什么能减少搜索 fan-out？缺少 routing 查询会怎样？
4. routing key 选择不当如何制造热点？你会监控哪些按分片指标？
5. `is_write_index` 解决什么歧义？为何别名切换要在一次 `_aliases` 调用完成？
6. 从 V1 迁移 V2 时如何追平增量、验证数据并设计回滚？
7. 面试场景：1000 个小租户和一个占 70% 流量的大租户共用索引。如何设计索引、routing 与容量？

加练：将实验分片数改为 3，记录 routing 前后 shard group 数和每分片文档数，并解释与原结果不同的原因。
