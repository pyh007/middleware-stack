# 生产者批处理、压缩与可靠性练习

1. `produce()` 返回成功与 delivery callback 成功分别说明什么？
2. `acks=all` 是否代表三副本都落盘？请列出必要配置和故障边界。
3. `linger.ms`、batch 大小和压缩如何同时影响吞吐、P99 延迟、CPU 与内存？
4. 幂等 Producer 能消除哪些重复，不能消除哪些重复？
5. 为什么只比较“发送 100 条耗时”不足以证明参数更优？应采集什么？
6. Broker 过载时无限重试有什么副作用？如何设计背压与失败出口？
