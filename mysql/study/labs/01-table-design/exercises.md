# 表设计练习题

先口述或写下答案，再查看 `answers.md`。

1. InnoDB 表为什么不宜随意使用很长的字符串作为主键？
2. 金额为什么通常使用 `DECIMAL(12, 2)`，`12` 和 `2` 分别代表什么？
3. `DATETIME` 和 `TIMESTAMP` 有哪些关键差别？跨时区系统如何选择？
4. 用户表做了软删除，并有邮箱唯一索引。注销用户重新注册相同邮箱时会发生什么？有哪些设计方案？
5. 外键有什么收益和成本？为什么有些大型系统不用数据库外键？
6. 状态字段应该使用 `ENUM`、数字还是字符串？给出你的场景和取舍。
7. 订单明细为什么通常要保存成交时的商品名称和价格，而不能只关联当前商品表？
8. 表中存在大量可空字段时，你会检查哪些业务或建模问题？
