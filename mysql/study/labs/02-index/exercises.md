# 索引练习题

1. InnoDB 聚簇索引和二级索引的叶子节点分别保存什么？
2. 什么是回表？什么是覆盖索引？覆盖索引是否一定更好？
3. 有查询 `WHERE tenant_id=? AND status=? ORDER BY created_at DESC LIMIT 20`，你会怎样设计索引？还需要哪些数据才能做最终决定？
4. 联合索引 `(a, b, c)` 能否服务只查询 `b` 的 SQL？为什么不能只回答“绝对不能”？
5. `WHERE a=1 AND b>10 AND c=3` 中，`c` 是否完全“失效”？
6. 为什么优化器有时不选择看起来正确的索引？
7. 区分度低的状态列是否永远不适合建索引？
8. 如何识别重复或冗余索引？删除前要验证什么？
9. 新增索引上线可能带来哪些风险？
