# SQL 调优练习题

1. 收到“数据库很慢”的反馈后，你首先收集什么信息？
2. `EXPLAIN` 和 `EXPLAIN ANALYZE` 有什么差别？后者为什么有风险？
3. 为什么 `WHERE DATE(created_at)='2025-02-15'` 往往不如时间范围？
4. 为什么按天查询通常写成 `>= 当天零点 AND < 次日零点`，而不是 `BETWEEN` 到 `23:59:59`？
5. `LIMIT 100000, 20` 为什么可能很慢？游标分页有哪些限制？
6. `SELECT *` 有哪些性能和工程问题？
7. 加完索引后 SQL 仍然慢，你会继续检查什么？
8. 如何证明一次 SQL 优化可以安全上线？
9. 慢日志中单次最慢的 SQL 是否一定最值得优化？
