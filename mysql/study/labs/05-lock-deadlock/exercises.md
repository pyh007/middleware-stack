# 锁与死锁练习题

1. 记录锁、间隙锁和临键锁分别保护什么？
2. 两个不同 id 的更新是否一定不会互相阻塞？
3. 如何从 `data_lock_waits` 找到根阻塞者？
4. 为什么缺少索引可能扩大锁影响？
5. 死锁和锁等待超时有什么差别？
6. InnoDB 选择死锁牺牲者后，应用应该重试哪一部分？
7. 统一资源访问顺序为什么能减少死锁？
8. `SELECT ... FOR UPDATE` 查不到记录时是否可能阻止其他事务插入？
9. 元数据锁造成的 DDL 阻塞应如何定位？
