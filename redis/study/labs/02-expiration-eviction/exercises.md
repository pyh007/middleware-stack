# 02 闭卷练习

1. `TTL` 返回 `-1` 与 `-2` 分别说明什么？为什么“到点就一定立即释放内存”不成立？
2. `volatile-lru` 与 `allkeys-lfu` 的候选集合和业务风险分别是什么？
3. `used_memory` 下降但 RSS 不下降可能是什么原因？需要联看哪些指标？
4. 百万会话键在同一分钟到期会引发哪些 Redis 和源站侧效应？如何用 TTL 抖动控制而不破坏业务有效期？
5. 预测 8 MiB 隔离实例写入 4 KiB Value 时 `evicted_keys`、存活键数和内存的变化。
6. 把压力实例改为 `noeviction`，预测客户端收到什么错误，以及应用应该如何降级；实验后恢复配置并清理实例。

