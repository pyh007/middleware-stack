# 04 闭卷练习

1. `INCR` 原子为什么不代表 `GET`→计算→`SET` 的整个流程原子？
2. MULTI/EXEC 遇到运行时命令错误会怎样？为什么不能说它像关系数据库一样自动回滚？
3. WATCH 冲突率很高时会出现什么副作用？如何限制重试风暴？
4. Lua 脚本为何能原子扣库存，又为何不应执行网络访问或长循环？
5. 锁只有 `SET NX PX` 而用普通 `DEL` 解锁会发生什么竞态？
6. 设计一个进程暂停超过锁 TTL 的故障注入，说明 fencing token 如何保护下游。

