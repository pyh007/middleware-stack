# 07 闭卷练习

1. 为什么用 JSON 字节数乘 Key 数会低估 Redis 容量？列出至少六类额外开销。
2. 有 120 个应用实例，每个池上限 50，滚动发布时最多双倍实例；如何和 maxclients 做预算？
3. `used_memory`、RSS、maxmemory、主机内存和容器限制之间是什么关系？
4. ACL 最小权限如何同时限制命令与 Key Pattern？凭据轮换怎样避免一次性中断？
5. DUMP/RESTORE 成功证明了什么，没有证明什么？
6. 为缓存、会话和幂等令牌分别定义一个合理的 RPO/RTO，并说明权威数据源与对账方式。

